# -AI-Agent-System-
# Команда: Агенты Смиты Тизер

Мы создаем платформу на базе искусственного интеллекта, которая поможет автоматизировать работу вашей IT-поддержки. Эта система не просто анализирует запросы – она встраивается в существующую инфраструктуру вашей компании и помогает сотрудникам решать технические проблемы быстро и без лишних хлопот.
Представьте, что ваши специалисты техподдержки получают мощный инструмент, который сам разбирает обращения, определяет, что именно нужно сделать, и даже выполняет рутинные операции. Это значительно ускоряет обработку обычных запросов, освобождая время для более сложных задач.
Технологии, которые мы используем: Python, LangChain, FastMCP, FAISS.

Что делает нашу систему особенной:
Понимание человеческого языка: Система не просто распознает ключевые слова, она понимает смысл запроса, как если бы с ней общался живой человек.
Безопасность: Все действия системы в вашей корпоративной сети абсолютно безопасны и соответствуют всем вашим требованиям безопасности.
Самообучение: Система постоянно учится на основе полученных данных и становится еще точнее и эффективнее с каждым новым запросом.
Сколько это стоит и когда будет готово:
Разработка пилотной версии обойдется в 2,2 миллиона рублей.
Нам потребуется 4 месяцев, чтобы создать и протестировать пилотную версию системы.

Что вы получите в итоге:
Сокращение времени обработки запросов.
Оптимизация работы службы поддержки.
Повышение удовлетворенности сотрудников.
Возможность сосредоточиться на решении стратегических задач.
Мы уверены, что наша система станет незаменимым помощником для вашей IT-команды и позволит вам значительно улучшить качество обслуживания пользователей. Эта платформа будет полезна всем сотрудникам, а IT-отдел сможет спокойно работать, занимаясь стратегическими проектами.
---

## 🚀 Быстрый старт

### Предварительные требования

* **Python 3.11+**
* **Docker** и **Docker Compose** (для контейнеризованного запуска)
* **API ключ** для VSEGPT или другой совместимой LLM

---

## 🧩 Локальный запуск (без Docker)

Клонируйте репозиторий и установите зависимости:

```bash
git clone <repository-url>
cd tech-support-system
pip install -r requirements.txt
```

Настройте переменные окружения:

```bash
cp .env.example .env
# Отредактируйте .env файл как описано ниже
```

Запустите систему:

```bash
python -m src.main
```

---

## 🐳 Запуск с помощью Docker

Соберите и запустите контейнер:

```bash
cp .env.example .env
docker-compose up --build
```

Для разработки с hot-reload:

```bash
docker-compose -f docker-compose.dev.yml up --build
```

---

## ⚙️ Настройка `.env` файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
# Обязательные настройки LLM
LLM_TOKEN=your_vsegpt_api_key_here
LLM_BASE_URL=https://api.vsegpt.ru/v1
NAME_LLM=mistralai/mistral-small-3.2-24b-instruct

# Настройки векторной модели
EMBEDDING_NAME_VECTOR_MODEL=sentence-transformers/paraphrase-multilingual-mpnet-base-v2
EMBEDDING_TYPE=huggingface
EMBEDDING_DEVICE=cpu  # используйте "cuda" если есть GPU

# Пути для данных
KNOWLEDGE_BASE_PATH=vector_store
DATA_PATH=data/data.md

# Настройки логирования
LOG_LEVEL=INFO
```

### 🔑 Где взять API ключ?

* **VSEGPT**: зарегистрируйтесь на vsegpt.ru и получите API ключ в личном кабинете
* **OpenAI**: используйте `https://api.openai.com/v1` в `LLM_BASE_URL` и укажите свой API ключ

---

## ⏱️ Время первого запуска

Первый запуск может занять **5–10 минут** из-за:

* загрузки моделей эмбеддингов (~500 МБ)
* создания векторной базы знаний
* инициализации детектора токсичности
* кэширования зависимостей

✅ Последующие запуски будут значительно быстрее.

---

## 🏗️ Структура проекта

```
src/
├── main.py                 # Главный файл для запуска
├── agents/                 # Реализации агентов
│   ├── DeEscalationAgent/
│   ├── IntentClassifierAgent/
│   ├── ToxicDetectorAgent/
│   ├── KnowledgeRetrievalAgent/
│   ├── ResponseGeneratorAgent/
│   └── KnowledgeBaseManager/
├── interface_agents/       # Абстрактные классы агентов
└── config.py               # Конфигурация приложения

data/
└── data.md                 # База знаний в формате Markdown

vector_store/               # Векторная база знаний (создается автоматически)
```

---

## 🔧 Конфигурация агентов

Система использует настройки из `config.py`:

```python
@dataclass(frozen=True)
class ConfigLLM:
    name: str = os.environ.get("NAME_LLM", "mistralai/mistral-small-3.2-24b-instruct")
    token: str = os.environ.get("LLM_TOKEN")
    base_url: str = os.environ.get("LLM_BASE_URL", "https://api.vsegpt.ru/v1")

@dataclass(frozen=True)
class ConfigVectorModel:
    name: str = os.environ.get("EMBEDDING_NAME_VECTOR_MODEL",
                              "sentence-transformers/paraphrase-multilingual-mpnet-base-v2")
    embedding_type: str = os.environ.get("EMBEDDING_TYPE", "huggingface")
    device: str = os.environ.get("EMBEDDING_DEVICE", "cpu")
```

---

## 📋 Функциональность

Система обрабатывает запросы пользователей через конвейер:

1. 🧠 **Детекция токсичности** — анализ на агрессивность и оскорбления
2. 🤝 **Деэскалация** — преобразование токсичных сообщений в конструктивные
3. 🎯 **Классификация намерений** — определение типа проблемы
4. 🔍 **Поиск в базе знаний** — RAG-система для нахождения решений
5. 💬 **Генерация ответа** — создание финального сообщения пользователю

### Поддерживаемые типы запросов:

* Проблемы с аутентификацией и паролями
* Технические неисправности
* Вопросы по тарифам и оплате
* Запросы контактной информации
* Жалобы и обращения

---

## 🐛 Устранение неполадок

### ❌ Ошибка: `ModuleNotFoundError`

```bash
pip install -r requirements.txt
# или
pip install --force-reinstall -r requirements.txt
```

### 🔑 Ошибка: `API key not found`

* Проверьте, что файл `.env` существует
* Убедитесь, что `LLM_TOKEN` корректен и активен
* Проверьте баланс на аккаунте API

### 💥 Ошибка: `CUDA out of memory`

* Установите `EMBEDDING_DEVICE=cpu` в `.env`
* Или используйте GPU с большим объемом памяти

### 🐢 Проблемы с производительностью

* Используйте `EMBEDDING_DEVICE=cuda`, если доступен GPU
* Увеличьте объем оперативной памяти для Docker
* Используйте более легкие модели эмбеддингов

---

## 🧾 Лицензия

Проект распространяется под лицензией **MIT**.

---

💡 *Создано с использованием LLM и современных технологий обработки естественного языка.*

## Архитектура решения

![img.png](img.png)

##
Демо версия работы системы:
https://disk.yandex.ru/i/bibm_p13xZLJQQ

